apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "demo-ms.fullname" . }}-test-connection"
  labels:
    {{- include "demo-ms.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "demo-ms.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
